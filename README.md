# MyDataScience
Репозиторий для практического изучения книги O'REILLY "Python для сложных задач"

## ЧАСТЬ I. JUPYTER: ЗА ПРЕДЕЛАМИ ОБЫЧНОГО PYTHON
1) Установка Miniconda
    - curl -O https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
    - bash Miniconda3-latest-Linux-x86_64.sh
2) Активация окружения source ~/.bashrc
3) Проверка окружения conda list

Запуск командной оболочки IPython

    ipython

    -Получение краткой информации о любом объекте при помощи '?'
    In [...] example? 

    - Получение информации и исходного кода при помощи "??"
    In [...] example??

    - Поиск определенной строки 
    In [...] str.*find*?   
             str.find   str.rfind

    
Запуск Jupyter Notebook
    jupyter lab

## Глава 2. Расширенные интерактивные возможности

-Выполнение внешнего кода **%run exapmple.py**
-Измерение времени выполнения кода **%timeit**
-Измерение времени выполнения блока кода **%%timeit**
-Документация магических методов **%magic**
-Простой список магических методов **lsmagic**

    In[...] --> list
    Out[...] --> dict 

    print(_) - доступ к предыдущим выводам _ - прошлый вывод __ - позапрошлый вывод ___ - максимальный вывод назад

    Быстрый доступ к Out[X] выглядит как _X 

**;** - Подавление вывода(результат не сохраняется в истории команд). Ставится в конце строки

**%history** - доступ к нескольких выводам
    example: %history -n 1-3 
    
**%rerun** - выполняют повторно заданную часть истории команд
**%save** - сохраняет какую-либо часть истории команд

Инструкции командного процессора в оболочке IPython
Для инструкции используется "!" перед командой
    example:
        In[...]: !ls
        In[...]: !pwd

Передача значений в командный процессор и из него
    example:
        In [...]: contents = !ls
        In [...]: print(contents)
        ['myproject.txt']
    example:
        In [...]: message = "Hello from Python"
        Для передачи из python используются фигурные скобки "{...}"
        In [...]: !echo {message}
            Hello from Python

Автоматические функции
    %cat, %cp, %env, %cd и т.д.
Их можно настраивать с помощью "магической" функции **%automagic**

## Глава 3. Отладка и профилирование

Управление исключениями: **%xmode**
    **%xmode Plain** - Простой
    **%xmode Context** - По контексту (default)
    **%xmode Verbose** - Подробный

Отладка:

    In [...]: %debug - В IPython сразу после ошибки
    
    ipdb: up   - Вверх по стеку 
    ipdb: down - Вниз по стеку
    ipdb: quit - Выход

**%pdb on** - Автоматический запуск отладчика

Профилирование

**%timeit** и **%time**

    %timeit - для 1 строки
    %%timeit - для блока кода

    %time - для 1 строки (1 раз)
    %%time - для блока кода (1 раз)

**%prun** - Встроенный профилировщик

    $ pip install line_profiler
    In [...] %load_ext line_profiler

**%lprun** - Пошаговое профилирование

Профилирование потребления памяти 

***%memit** и **%mprun**

**%memit** - Профилировщик памяти

    $ pip install memory_profiler
    In [...] %load_ext memory_profiler

**%mprun -f** - Пошаговый профилировщик памяти

## ЧАСТЬ II. Введение в NumPy
